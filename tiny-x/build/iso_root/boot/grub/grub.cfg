set timeout=3
set default=0

menuentry "tiny-x OS" {
    multiboot2 /boot/kernel
    module2    /boot/initramfs.cpio initramfs
    boot
}

menuentry "tiny-x OS (serial debug)" {
    multiboot2 /boot/kernel console=ttyS0
    module2    /boot/initramfs.cpio initramfs
    boot
}
